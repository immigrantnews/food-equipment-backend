# Деплой статики/инфры (не в Docker)
# nginx:   scp deploy/nginx.conf root@server:/etc/nginx/sites-available/indmart && nginx -t && systemctl reload nginx
# robots:  scp deploy/robots.txt root@server:/var/www/html/robots.txt
